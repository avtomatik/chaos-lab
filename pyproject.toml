[project]
name = "cobweb-plot"
version = "0.2.0"
description = "Python laboratory for discrete dynamical systems, simulations, and visualization."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.1",
    "pandas>=3.0.5",
]
